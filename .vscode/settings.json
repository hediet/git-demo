{
    //"git.branchProtection": ["master"],
    "git.branchProtectionPrompt": "alwaysCommitToNewBranch",
    "githubPullRequests.queries": [
		{
			"label": "Merged",
			"query": "repo:${owner}/${repository} is:closed is:merged author:${user}"
		}
	],
}